import express, {
  ErrorRequestHandler,
  NextFunction,
  Request,
  Response,
} from "express";

// const app = express();
// app.use(cors("*"));
// app.use(express.json());

// app.use("/api/dashboard", dashboardController());

// export default app;
